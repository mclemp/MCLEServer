#pragma once

class Command;
class MinecraftServer;

class CommandRegistry {
public:
	//static bool OnConsoleCommand(MinecraftServer* Instance, const std::wstring& rawCommand);
private:
	//static std::vector<std::shared_ptr<Command>> registeredCommands;
};

